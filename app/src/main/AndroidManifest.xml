<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="atos.net.interestingplaces" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <service
            android:name="com.octo.android.robospice.Jackson2SpringAndroidSpiceService"
            android:exported="false" />

        <activity
            android:name=".ui.SplashActivity"
            android:label="@string/title_activity_splash" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.BaseActivity"
            android:label="@string/title_activity_base" >
        </activity>
        <activity
            android:name=".ui.PlacesList"
            android:label="@string/title_activity_places_list" >
        </activity>
        <activity
            android:name=".ui.PlaceDetails"
            android:label="@string/title_activity_place_details"
            android:parentActivityName=".ui.PlacesList" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="atos.net.interestingplaces.ui.PlacesList" />
        </activity>
    </application>

</manifest>
